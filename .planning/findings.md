# 阶段三增量发现

- 500215 是彩蛋触发节点，背景图为 `b2956350925fec64a8cdf5853d439c7c`，已存在于对白数据中。
- `CampaignMapUI` 仅会为 `ChapterDefinition.beatNodeIds` 中、且已访问的节点创建战况卡，因此将 500215 加入第五幕数组即可复用现有卡片样式与图片渲染。
- 剧情回顾入口由 `CampaignMapUI` 运行时创建；`CloseMap()` 会默认重新显示入口，所以返回主菜单必须显式关闭入口，开始新局再显式恢复。
- 当前 `VisualDirector` 连续色调 alpha 较低，用户反馈“完全没看出来”，需要增加短时事件闪变而不是只提高常驻遮罩。
- 终局画面原有“剧情回顾”入口由 `CampaignMapUI` 动态创建；终局隐藏该入口后，不能复用它承担终局复盘，否则玩家会失去明确的结局操作语义。因此新增按钮应克隆场景中的 `endingReturnButton`，保持同一 Sprite/ButtonSpriteSwap 视觉资源。
- `CampaignMapUI` 的章节卡在普通回顾中只为当前幕展开分支。终局复盘需要回看每幕实际选择，所以已在 completed summary card 的底部加入一行 `本局选择：...`，不重新展开全部分支，避免回到密集节点图。
- 七条评价映射使用解锁节点与终止节点成对归并；评价卡只由 `endingReviewMode && HasReachedEnding` 触发，因此普通回顾不会提前显示评价，也不会泄露未来结局。
- 3B 首批史官注适合挂在五幕章节卡而不是每个战况 beat 上：五个决策锚点正好对应玩家需要做出取舍的时刻，信息量可控，也不会把回顾重新膨胀成节点图。
- 史官注采用运行时弹层和内置文本，关闭时只恢复 recapPanel；这样不需要新增场景引用或美工资源，也避免弹层关闭导致滚动卡片和选择状态重建。
- 当前史官注弹层把分区标题和正文拼在同一个 TMP 文本对象中，导致“史书记载”“今天如何理解”“游戏改编说明”无法独立设置字号与颜色。
- 本轮将拆分为独立的标题/正文文本对象，采用暖金色标题与更大的字号层级；正文保持白色，来源行使用次级灰蓝色，确保信息层级和可读性。
- 本轮复核发现回顾列表只在 `isOpen` 时响应节点事件；事件到达时若面板刚打开/刚切幕，重建可能落后于实际访问状态。当前章节判定还只读取 `CurrentNode.nodeId`，需同时使用已访问节点范围兜底，避免第四幕等跨节点转场时暂时停留在第一幕。
- `DialogueSystem.ShowDialogueNode` 会先写入 `CurrentNode` 和 `visitedNodeIds`，再依次触发 `OnNodeTransition`、`OnDialogueNodeShown`；因此回顾可用“当前节点 + 已访问节点最高幕次”判定，补偿特殊节点/小游戏转场时的短暂旧节点状态。
- 史官注底部改编说明使用固定 36 号且允许换行，长句容易产生末行孤字；改用禁换行的 TMP 自适应字号（36→26）后，优先保持单行并避免孤字，仍保留省略保护。
- UI/UX Pro Max 的语义检索脚本在当前环境没有可用的 Python/py 运行时，未取得可验证的数据库匹配；本轮按其通用文本层级与模态可读性原则，结合 Unity 桌面端实际尺寸完成实现。
- 阶段三已获用户实机验收，阶段四正式转为“文化信息扩充 + 邀约式共创 MVP”。为保持 NFR-031/NFR-034，首版采用离线同构建/同设备的应邀流程，不把后端、账号或第三方网络服务作为前置条件。
- “邀约式”不应只是分享按钮；最小可演示闭环需要邀请快照、邀请码、应邀者策略建议、主玩家确认/拒绝和带署名的共谋回声卡，并且这些状态与原始剧情路线隔离。
- 现有 UI 入口集中在 `CampaignMapUI`（普通/终局回顾）与 `FinalUIManager`（游戏界面生命周期）；阶段四适合新增独立运行时 `InviteCoCreationUI`，由 `FinalUIManager` 创建并让 `CampaignMapUI` 提供当前回顾快照，避免把邀约逻辑继续塞进卡片绘制方法。
- `ResourceManager` 已提供四项资源的只读 getter（兵力/粮草/计策/风险），可直接用于邀请快照；`CampaignMapUI` 当前选择记录仍是私有字典，后续应通过只读摘要接口提供给邀约模块，而不是暴露可写集合。
- 阶段四任务二已落地：`CampaignMapUI` 注册“军议邀约”入口并提供 `TryBuildInviteSnapshot`，`InviteCoCreationUI` 运行时创建快照卡、复制邀请码按钮和 FNV-1a 8 位码。入口仅在抵达决策锚点后出现，地图关闭/返回主菜单时通过 `OnClosed` 清理会话。
- 任务二首批实现暂不接受应邀建议，避免把未验收的双角色状态提前接入主线；下一任务继续在同一面板增加邀请码输入和策略建议提交。
- 2026-09-08 用户确认继续下一批。按既有路线，当前应从阶段四 4A 第二批开始，优先顺序为“本地存档骨架与安全恢复 → 首页存档入口 → 关于游戏 → 基础设置”，而不是直接进入需要公网服务的 4C。
- 当前运行状态分散在 `DialogueSystem` 的当前节点/访问顺序、`ResourceManager` 的四项资源、`CampaignMapUI` 的实际选择记录及各结局管理器中。读档不能仅调用 `ShowDialogueNode`，否则可能重新触发节点展示与资源逻辑；需要显式的快照导出/恢复入口，并在 UI 层完成一次无副作用重绘。
- `DialogueSystem.ShowDialogueNode` 本身不会再次扣除选项资源，但会把节点再次追加到访问顺序、切换 BGM/SFX 并发出事件；安全读档应先批量恢复访问集合/顺序和资源，再用专用恢复方法设置当前节点并只触发一次必要的显示事件。
- `CampaignMapUI.selectedOptionIndices` 是当前唯一可靠的“锚点→实际选项索引”记录，现为私有运行时字典；4A 存档必须为它增加只读导出与受控恢复方法，否则读档后回顾中的“本局选择”会丢失。
- 首页 `StartGame()` 已经过“出征之前”说明再调用 `BeginNewRun()`，适合新增独立的运行时首页扩展 UI；已有场景资源无需改动即可先挂接“查看存档 / 关于游戏 / 设置”入口。
- 首页场景当前只有一个 `StartButton`，尺寸 300×100，使用 `Assets/Art/未选中.png` 的浅色卷轴按钮资源；4A 第二批可克隆该按钮生成一致的首页入口，避免直接修改脏场景 YAML。
- `AudioManager` 位于 `Assets/AudioManager.cs`，不在 Managers 目录。设置页需基于它现有 AudioSource 音量接口扩展，不能假设项目使用了 AudioMixer。
- `VisualDirector` 对每次资源事件都会脉冲数字；恢复资源时应提供“静默批量恢复 + 单独刷新”语义，避免读档瞬间播放四次资源变化动画。
- 终止节点稳定 ID 为 200314、300416、500217、500313、500320、500411、500418；对应解锁提示节点不应当直接视作“通关归档”，自动通关标题应只在真正终止节点生成。
- 首页按钮的场景 `StartButton` 已使用 `Assets/Art/未选中.png`，可在运行时克隆并重绑事件；这样新增入口能继承现有字体、Sprite 和交互反馈。
- `FinalUIManager.OnDialogueNodeShown` 是恢复后重绘背景、人物、对白、选项和终局按钮的统一入口；`DialogueSystem.RestoreRunState` 只需恢复数据并发出一次该事件即可复用现有展示链。
- 500308 与 500406 是会立刻根据资源二次跳转的路由节点，不适合保存为可恢复的当前安全节点；自动档应跳过这两个节点，防止读档后再次分流和重复写访问顺序。
- 当前阶段四校验脚本只编译既有五个 UI 脚本；新增存档脚本后要扩充校验输入，并避免依赖陈旧 `Assembly-CSharp.dll` 中的同名类型来掩盖接口错误。
- 2026-09-08：存档恢复链已经采用“静默恢复”而非重放剧情：资源走 `RestoreSnapshot`，剧情访问序列走 `RestoreRunState`，存档管理器不会调用选项执行或资源增减方法；500308、500406 两个纯路由节点被排除出自动检查点，避免读档后停在瞬时跳转节点。
- 2026-09-08：存档界面已覆盖首页读取入口、局内保存入口、自动检查点和 3 个手动槽；存档文件带版本号、剧情版本号、备份文件及坏档保留提示。当前定向编译已通过，下一步要把这些关键约束写成自动验收，避免后续改 UI 时破坏读档安全。
- 2026-09-08：资源管理器现有重置入口名为 `ResetAllResources`；存档契约检查最初按旧命名 `ResetResources` 截取方法而失败，属于验收脚本边界错误，生产代码本身的静默恢复逻辑正常。
- 2026-09-08：项目绑定的 Unity 2022.3.62f3c1 编辑器存在，可尝试批处理项目编译；若同一项目正在前台 Unity 中打开，批处理会因项目锁退出，不能据此判定代码失败。
- 2026-09-08：Unity 批处理项目检查启动后超过一分钟仍无日志输出，疑似前台编辑器占用项目或 Unity 启动阶段阻塞；不应无限等待或强行关闭用户的编辑器，项目级编译结果需改由现有编辑器控制台/用户实机确认。
- 2026-09-08：首页按钮可以继续复用现有 StartButton 的美术与交互；`AudioManager` 已有持久化 BGM 音量，但把接近静音的保存值强制重置为 1，且音效/UI 音量尚未持久化。设置面板需要修正静音恢复并补总音效音量；亮度宜采用 UI 遮罩实现，画质与全屏直接走 Unity 本地设置。
- 2026-09-08：场景中的 StartButton 位于首页中心 `(x=-1,y=-218)`、尺寸 `300×100`；现有“查看存档”在 `y=-338`。为避免纵向按钮超出 1080p，关于游戏可放 `y=-438`，设置采用右上角独立小入口；两者继续克隆现有按钮美术。
- 2026-09-08：新建的四个 Unity C# 文件尚未由前台编辑器生成 `.meta`；已补入稳定 GUID 的 MonoImporter 元数据，避免换机、提交或重新导入后引用漂移。定向 `git diff --check` 对本轮已跟踪代码无新增空白错误。
- 2026-09-08：4B 的规格边界已明确：战绩报告必须复用真实本局日志，展示结局、实际决策、四资源趋势、可解释的本局倾向、文化说明与参谋记录；不得补画未经历幕次、虚构百分位或全国统计。PNG 首版为本机导出，不需要游戏上线，也不放假二维码。
- 2026-09-08：`DialogueSystem.HandleOptionSelected` 的顺序是条件校验 → 四资源实际结算 → `OnOptionSelected`，因此日志可在既有选项事件中取得真实结算后状态；资源事件先于选项事件逐项发出，可暂存首个旧值并在选项事件合并为一条决策，外部小游戏资源变化则在帧末单独落成趋势点。这样无需改动选项事件签名，也不会影响现有回顾订阅者。
- 2026-09-08：五个锚点真实选项已核对：1001 三选（发石车/地道/诈败）、2001 二选（坚守/回许都）、3001 三选（坦诚/试探/斩首）、4001 二选（亲征/遣将）、5001 三选（攻乌巢/回守/直取袁绍）。本局倾向可由选择文本中的“坚守/回守”“地道/埋伏/奇袭”“亲自/全力/直取”“采纳/坦诚/遣将”等可核对证据归纳为稳慎、奇谋、果决、纳谏，允许同一选择贡献多个倾向。
- 2026-09-11：本轮确认 Unity 编辑器未运行；`Assembly-CSharp.dll` 时间戳为 23:06，静态合同检查通过。最新代码将邀约入口设为 300×80，并从选项运行时同步字号/字重；视觉截图仍需用户重新打开 Unity 后验收。
- 2026-09-11：针对新截图进一步确认：截图中的邀约位置与旧 `.40` 锚点一致，说明旧程序集/场景仍在运行；尝试批处理启动时主窗口标题为 `Sentinel LDK Protection System`，停在授权阶段，未进入项目导入。最新源代码改用 `.44` 布局兜底 + 每帧重试对齐，并把白字守护组件设为高执行顺序。
- 2026-09-11（解除阻塞后）：为使场景重载即使暂时沿用旧程序集也不会再触发黑字，`StartButton` 的旧 `ButtonSpriteSwap` 组件已在场景中禁用；Button 组件本身与 TMP 字色仍保留，静态检查直接验证该禁用状态。
- 2026-09-08：终局现有“返回主菜单”在上、“本局复盘”克隆按钮在 `(0,-330)`；4B 可把复盘与战绩报告调整为同一行左右排列并复用同一按钮资源。结局图片、名称、摘要和七条评价已由 `CampaignMapUI` 统一解析，报告应通过只读公开包装复用它们，避免再复制一套结局映射。
- 2026-09-08：4B 报告入口已安排为终局三按钮：返回主菜单在上，下面左右为“本局复盘”和“战绩报告”；报告滚动页顶部先放结局图/评价、游玩统计、决策倾向和四资源图，实际决策、史官简注、军议记录继续向下滚动，确保海报首屏包含核心信息。
- 2026-09-08：报告趋势图首屏布局已按“结局/统计/倾向/图表”压缩到可见区域，实际决策和文化说明下滚动；PNG 导出前强制回到报告顶部，因此海报不会截到玩家无关的主菜单或报告底部状态提示。
- 2026-09-08：独立分析器回归通过：单独的“挖掘地道，奇袭袁营”会归纳为“奇谋”并保留原选项作为证据；空日志返回“尚待观察”，不会凭空生成画像。报告契约还确认七个结局各有文化说明，且不含排名、百分位、二维码等未落地能力。
- 2026-09-08：终局按钮只会在七个真正结局节点显示；七个“解锁提示”节点仍先走原有“查看结局”流程，进入对应终止节点后才显示“返回主菜单 / 本局复盘 / 战绩报告”三按钮，避免报告提前把未完成的解锁提示当成完整结局。
- 2026-09-08：实机返工确认首页附加按钮不能继续 Instantiate StartButton，因为 RemoveAllListeners 不会清除序列化的持久 StartGame 监听；本轮所有动态首页入口和终局操作按钮均改为新建 GameObject。
- 2026-09-08：项目未安装 Unity Vector Graphics；齿轮、喇叭/静音、退出箭头采用本地 UGUI 几何图形，外形参考 Bootstrap Icons 官方 MIT 图标库，不依赖运行时网络或新增包。
- 2026-09-08：退出保存采用自动检查点并复用既有安全节点校验；当前不可安全保存的瞬时节点不会强行写档，确认框会保留失败提示，直接退出仍可返回首页。
- 2026-09-08：战绩报告首卡改用已绑定的曹操头像，资源趋势图单点居中并拉出短线、节点放大；导出完成后给出完整路径、打开图片、打开文件夹和复制路径操作，不伪造系统社交分享已完成。
- 2026-09-08：CUA 当前没有 Unity 可操作应用句柄，因此本轮只能完成定向编译和静态/逻辑契约检查，首页、五锚点和七结局的像素级布局仍需用户在 Unity Game 视图集中确认。
- 2026-09-10：附件对应的 Unity 编辑器实例由 `Library/EditorInstance.json` 指向本项目，但 `Library/ScriptAssemblies/Assembly-CSharp.dll` 停留在 21:03，晚于它的源码修改未被该实例编译；无界面批处理因启动/许可阶段无日志输出而无法完成刷新。已将确认无运行实例后的零字节 `Temp/UnityLockfile` 移到可恢复备份，未删除项目数据。新建的 `StartButtonLabelColorFix` 与运行时邀约 RectTransform 对齐代码仍通过定向编译和契约检查，最终像素级验证必须在 Unity 重新打开并完成导入后进行。
- 2026-09-11：终局重开路径原先复用 `StartGame()`，会重新创建身份说明弹层并依赖旧的 gameplay 子面板状态。新增 `StartReplay()` 直达 `BeginNewRun()`，同时重绑对白/背景介绍点击监听、将点击层置顶并清除 EventSystem 选中对象，针对“背景介绍后首个对白画面不可点击”做生命周期兜底。
- 2026-09-11（最新截图复核）：截图仍呈现旧 `.40` 邀约锚点和黑色开始游戏文字；当前场景 YAML 已确认 TMP 白字、旧 `ButtonSpriteSwap` 禁用、邀约 `.44` 兜底及运行时实测间距均已落盘。运行中的 Unity `Assembly-CSharp.dll` 时间仍早于本轮源码，故该画面不能作为最新代码的视觉验收证据。
- 2026-09-11：4C 第一增量采用同一设备/同一运行实例的本机接力码，不把确定性短码当作跨设备凭证。会话状态机和邀请码只存在运行时；主将采纳建议后记录 `RunHistoryData.advisorEchoes`，原剧情选项仍是唯一推进入口。
- 2026-09-11：邀约面板保留“关闭后回到快照”的状态，以便主将审核后回到当前节点；真正离开本局的边界由新局、读档、返回主菜单和撤销邀约显式清理。这样关闭弹层不会丢失已确认的共谋回声，同时不会阻塞单人路线。
- 2026-09-11：第二次打开应邀面板时输入框无法点击，静态检查发现运行时创建的 `TMP_InputField` 没有显式 `targetGraphic`，且重开时可能残留 EventSystem 选中对象。补齐背景图目标、可交互/非只读状态，并在每次打开时清除旧选中对象和重置光标位置；理由字段只需非空对象即可，空文本合法。
- 2026-09-11：进一步将应邀面板的恢复顺序固定为“激活父面板 → 刷新画布 → 重置输入组件”，并在关闭/回顾关闭时清理选中对象，以覆盖 TMP 输入组件被隐藏后再次启用的生命周期。
- 2026-09-11：`InviteSessionState.SubmitSuggestion` 现在把 `null` 理由规范化为空字符串，避免非 UI 调用路径误把可选理由当成必填。
- 2026-09-11：战绩报告的共谋回声理由原先与采纳句同一行，已改为独立换行；结局英雄卡摘要改为居中，便于与曹操头像形成对齐。
- 2026-09-11：为阶段推进补充五锚点状态机回归（1001/2001/3001/4001/5001），确认不同选项数量下空理由、采纳和重置均保持一致；Unity UI 路径仍需实机确认。
- 2026-09-11：Unity Play Mode 已完成一次完整本机邀约烟测：昵称输入可用、空理由提交合法、主将采纳后关闭审核仍回到决策并保留邀约/回顾入口；但从采纳后的发石车小游戏继续时进入拼图界面，属于主线小游戏路径，不能据此冒充五锚点/七结局完整回放。
- 2026-09-11：Unity Play Mode 追加真实终局路径：首个决策选择“伴装败退，设下埋伏”并完成诱敌滑杆后进入撤退失序结局；战绩报告头像旁显示完整摘要且居中，资源趋势右侧百分比可见。仅覆盖一条终局路径，五锚点与七结局完整回放仍待验。
- 2026-09-12：同一 Play Mode 会话补测 2001 第二选“粮草乃根本，不如暂回许都，再图后举”，通过 2003x 终局叙事后打开战绩报告；实机再次确认曹操卡完整摘要无省略号并居中，趋势条右侧百分比可见。该证据只扩展到第二锚点的一条分支，不能替代五锚点与七终局全回归。
- 2026-09-12：在上述终局菜单实际点击退出入口→“再玩一局”，新局完成背景介绍后进入首个角色对白，点击继续可正常推进到地图事件；这条回放确认 `StartReplay` 的面板/点击层复位覆盖了此前重开后不可操作画面。仍需跨全部锚点与终局做完整回归。
