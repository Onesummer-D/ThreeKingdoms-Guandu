# 工作进度

## 2026-08-30 · 阶段三增量启动

- 建立本增量规格、路线和发现记录。
- 500215 已加入第五幕回顾，并在终局保留最近两张代表性战况卡。
- 剧情回顾入口已加入返回主菜单隐藏、开始新局恢复的显式生命周期控制。
- VisualDirector 已加入可见的红/金资源事件闪变，并提高常驻氛围、文字提示色和数字脉冲强度。
- 使用 Unity 2022.3 自带 Roslyn 对三个改动脚本完成独立编译检查，0 error；编辑器当前仍无可见窗口，实机截图待用户打开 Unity 后复核。
- 下一步：Unity 窗口恢复后做一次实机截图验收，再决定是否进入 3B 史官注。

## 2026-08-30 · 终局复盘入口与评价卡

- 终局画面隐藏普通“剧情回顾”入口，运行时克隆“返回主菜单”按钮生成“本局复盘”，按钮置于其下方并复用同一套图片、字体和悬停反馈。
- 复盘模式保留五幕已走过的代表性卡片，并在每个已完成章节摘要底部显示“本局选择”；未访问章节仍不创建卡片。
- 结局卡之后新增“本局复盘 · 总体评价”卡，七条结局线分别使用独立文案，并复用对应结局图片回显路线。
- `CloseMap` 增加关闭事件，终局复盘关闭后恢复专属入口；回主菜单或重新开始时清理专属入口，普通入口只在新局恢复。
- Unity 2022.3 自带 Roslyn 对 `FinalUIManager.cs`、`VisualDirector.cs`、`CampaignMapUI.cs` 独立编译 0 error；仅有临时引用旧 `Assembly-CSharp.dll` 的 CS0436 类型覆盖警告。编辑器无可见窗口，尚未做实机截图。
- 当前停点：先请用户在 Unity 里检查终局双按钮、复盘选择行和评价卡；确认后再进入 3B 史官注与 3C 回归封板。

## 2026-08-30 · 3B 史官注与 3C 回归清单

- 五个决策锚点（1001、2001、3001、4001、5001）新增“史官注”按钮，仅在锚点已访问后出现。
- 史官注弹层提供“史书记载、今天如何理解、游戏改编说明、参考来源”四个字段，可从普通回顾和终局复盘打开，关闭后恢复原回顾面板。
- 新增 `Documentation/阶段三3C回归清单.md`，覆盖新局、史官注、终局双按钮、终局复盘、主菜单、重新开始和资源闪变十项流程。
- Unity 2022.3 自带 Roslyn 对三个改动脚本编译 0 error；静态回归检查通过：史官注 5/5、评价映射 7/7、3C 流程 10/10、无剧情数据写入。
- 当前停点：等待 Unity Game 视图实机跑通 3C 清单并确认弹层排版、按钮位置和资源闪变强度；之后进入全国赛展示封板。

## 2026-08-30 · 史官注可读性调整

- 将“史官注”标题放大至 64，三个分区标题统一为暖金色 44 号，正文提升至 36 号，来源行提升至 30 号。
- 分区标题与正文改为独立 TMP 文本对象，避免同一行拼接导致字号、颜色无法区分；弹层布局同步压缩空隙，保留中文换行空间。
- Unity 2022.3 自带 Roslyn 对三个改动脚本编译 0 error；待 Unity Game 视图恢复后确认实际换行与 1920×1080 可读性。

## 2026-08-31 · 回顾同步与改编说明换行修复

- 修复回顾在第四幕等特殊转场时暂时只显示第一幕的问题：幕次由 `CurrentNode` 与 `visitedNodeIds` 的最高已访问幕次共同推导，避免小游戏/过渡节点短暂持有旧节点时丢失已走内容。
- 增加回顾打开期间的轻量状态兜底同步，监听已访问数量和当前节点 ID 变化；不改动剧情数据，仅触发 UI 重建。
- “游戏改编说明”正文改用 TMP 单行自适应字号（36→26），优先保持一行，超长时平滑缩小并保留省略保护，避免第二行只剩一个字。
- Unity 2022.3 自带 Roslyn 对三个改动脚本编译 0 error；待 Unity Game 视图按第四幕转场和史官注长句复核实际效果。

## 2026-09-01 · 阶段三验收通过，阶段四启动

- 用户已确认阶段三实机效果通过：第四幕回顾同步、史官注排版、彩蛋/结局卡、终局复盘入口、主菜单隐藏和重新开始流程均满足当前目标。
- `Documentation/阶段三开发记录.md` 已补充最终验收结论，并将下一停点转为阶段四。
- 规格升级至 1.3，新增 FR-040–046 / NFR-034–036；阶段四计划已建立，Active Phase 切换为 `04-invite-co-creation`。
- “邀约式”安排在阶段四立即实现，先做离线同设备双角色接力 MVP：军议邀请 → 邀请码 → 应邀建议 → 主玩家确认/拒绝 → 共谋回声卡。任务 2–4 完成后即可录制展示，不等到阶段五。
- 阶段四任务一已完成：确定 `InviteSessionState` 字段、FNV-1a 确定性邀请码规则、状态迁移和主线隔离边界；下一任务进入“军议邀约”入口与邀请卡 UI。

## 2026-09-04 · 阶段四范围修订与 4A 第一批

- 用户提出八项体验与社交扩展，规格升级 1.4，记录完整落地方案。旧短码仅为本地编号，未实现加入；暂停该方案，不再以旧任务二状态宣称邀约可用。
- 邀请正文改为自适应高度、滚动阅读，标题与底部操作独立；复制完整战局、身份、资源与当前已知问题，明确微信人工回复、尚未自动回传。
- 新局增加曹操身份说明，确认才重置并出征；本次不扩写许攸、袁绍独立路线。
- 找到幕次问题的直接原因：原第一幕 1001–199999 区间吞入四位决策锚点。新增统一分类函数并修复高亮；此前事件同步解释并非完整根因。
- 目标五脚本独立编译通过；生产分类函数 36 项检查通过；204 个资源节点无未分类 ID。首次测试误引用原生 DLL，改为必要托管引用后通过。
- 全仓 diff 检查发现已有场景 YAML 空字段行尾空格，本轮未改该场景；不擅自清理用户改动。
- Unity 画面/复制/返回实际操作尚未验证；本批保持 partial，提供实机清单。下一批为存档、关于游戏和基础设置，文化内容与后续战绩报告同步推进。

## 2026-09-08 · 4A 第二批启动

- 用户确认进入下一阶段；4A 第一批按多轮实机反馈收口，当前 Active Task 切换为 `03-local-save-core`。
- 下一阶段先交付本地存档核心与可见槽位入口，再接关于游戏和真实生效的基础设置；公网邀约、社区和账号仍不在本批。
- 已确认存档恢复必须覆盖节点访问顺序、锚点实际选择与四项资源，且不能通过重放选项恢复状态。

## 2026-09-08 · 4A 第二批代码完成，待实机验收

- 新增版本化本地 JSON 存档：1 个自动检查点、3 个手动槽、临时文件替换、上一次备份、坏档/版本不兼容提示与原文件保留。
- 存档覆盖当前节点、访问顺序、实际选项、四项资源、IF 解锁和结局；读取采用静默状态恢复，不重放选项、不重复扣资源。
- 终局节点自动保存为“通关结局 X：名称”；500308、500406 两个瞬时路由节点不生成检查点。
- 首页新增“查看存档”“关于游戏”和右上角“设置”，局内新增“保存进度”；关于页包含人物与玩法双卡，并明确当前只开放曹操视角。
- 设置已真实接入背景音乐、音效、亮度、全屏和 Unity 画质等级并持久化；同时修复保存为静音后重启又恢复满音量的问题。
- 定向编译、36 条章节检查、存档安全契约和首页设置契约全部通过。Unity 批处理因前台编辑器占用且无日志被停止，未关闭用户编辑器；下一步由 Game 视图实机验收。

## 2026-09-08 · 4B 战绩报告代码推进完成，待实机验收

- 新增真实本局日志：记录有效游玩时长、已显示节点、每次实际选项、选项前后资源、资源变化节点和原因；同一节点/选项重复事件有保护。
- 日志进入自动档与手动档，读档恢复日志但不重放剧情；报告只读取本局数据，不补画未经历路线。
- 新增终局“战绩报告”入口，与“本局复盘”并排显示；报告包含结局图/名称/评价、游玩统计、实际决策、四资源 0—100 趋势线、史官简注和参谋记录占位。
- 新增“本局决策倾向”：根据真实选择归纳稳慎、奇谋、果决、纳谏，并直接列出选择证据，不生成排名、百分位或心理诊断。
- “分享海报”会把当前报告首屏导出为真实 PNG，保存到用户图片目录；不自动发布、不带账号信息、不生成无效二维码。
- 定向编译、36 条章节检查、存档/首页/日志/报告契约检查全部通过；下一步是 Unity 完整路线实机验收并确认 PNG 可打开。

## 2026-09-08 · 4B 体验整合返工完成代码，待集中实机验收

- 按用户 17 项实机反馈升级 spec 1.5，并建立 `06-ux-consolidation` 任务计划。
- 首页三入口改为等尺寸横排，设置改为齿轮与全局静音图标；旧右侧 BGM 滑条启动时隐藏。画质改成“省电/标准/超清（Ultra）”三档，显示帧率、抗锯齿、纹理策略；全屏状态在编辑器和打包后分别给出真实提示。
- 修复首页附加按钮继承 StartGame 持久监听的问题；关于页改为认识人物/玩法卡片，人物页展示曹操、许攸、袁绍立绘与关键词，玩法说明和三个设置百分比放大。
- 移除局内旧保存按钮，新增全程右下角退出箭头和“保存进度/直接退出”确认；军议邀约从回顾入口移到五个决策点问题旁，快照删除重复幕次和冗余邀请话术。
- 终局仅保留并排的本局复盘/战绩报告；报告改用曹操头像、放大文字和资源趋势线，单点数据居中显示，架空声明移到框底，移除 4C 占位语句。
- PNG 导出成功后提供完整路径、打开图片、打开文件夹和复制路径；不伪造社交平台已分享。当前 CUA 无 Unity 应用句柄，视觉验收留给用户下一轮 Game 视图确认。
- `VerifyPhase4.ps1` 全部通过：定向 UI 编译（无编译警告）、36 条章节检查、战绩分析、存档、首页设置、运行日志、报告契约。

## 2026-09-08 · 4B 第二轮截图修订完成代码，待视觉复核

- 依据新一轮 12 项截图反馈升级 spec 1.6：关于/设置/报告正文不再用省略号隐藏内容，关于双卡改为深色金边交互卡且标题居中加粗。
- 设置页扩大百分比与全屏文字，白色滑块统一为正方形并移除两行辅助状态；首页齿轮、喇叭与退出箭头增加本地生成图标 Sprite，避免出现空白白块。
- 强制在剧情节点和终局关闭 StartMenuPanel，恢复局内“剧情回顾”并将地图入口置于最上层；军议邀约锚到对话框内问题右侧。
- 终局只保留两枚终局入口；战绩报告标题改为“战绩报告”，倾向说明居中且移除“依据”前缀，图例改为两行布局并扩大趋势图区域。
- 趋势图兼容旧存档：优先读取真实资源时间线，缺失时从实际决策前后快照重建；史官简注改为对应历史史实结局说明。
- `VerifyPhase4.ps1` 全部通过：定向 UI 编译、36 条章节检查、战绩分析、存档、首页设置、运行日志、报告契约。当前仍只差 Unity Game 视图视觉确认。

## 2026-09-08 · 第二轮卡片与契约收口

- 关于页双卡改为纯深色金边信息卡，标题居中加粗，正文居中并保留自动缩放；不再拉伸旧纸牌底图。
- 报告契约新增旧标题与“依据：”回退保护；首页契约新增卡片居中/加粗保护。
- 本轮修改后 `VerifyPhase4.ps1` 再次全部通过；视觉状态仍保持 `awaiting_visual_verification`，不虚报 Unity 实机通过。

## 2026-09-09 · 第三轮截图修订完成代码，待视觉复核

- 依据新截图将首页三按钮文字统一为 42 号并统一白色底，关于玩法末句补句号；设置页三项标签/百分比缩小，滑块手柄改为 24×24，画质档去掉 Ultra。
- 退出箭头改为真正右下角锚点；军议邀约改挂在 GameInterfacePanel，置于三项决策按钮下方，避免压住对话文字。
- 战绩报告倾向说明上移到卡片中心；移除“01/02”决策卡；史实简注居中；趋势区增加四条原生 UGUI 资源进度条和实际数值，避免自定义趋势图在 Game 视图中呈空白。
- `VerifyPhase4.ps1` 已再次全部通过；当前计划仍保持 `awaiting_visual_verification`。

## 2026-09-10 · 第三轮交互与坐标收口

- 首页明确区分按钮语义：开始游戏使用棕色底，查看存档/关于游戏使用白色底，三者统一深色 42 号文字；玩法末句保留完整句号。
- 设置页移除全屏勾选项并强制 FullScreenWindow；画面模式文字放大到 44 号，三条滑块手柄缩至 18×18，继续显示完整百分比。
- 局内退出的保存动作改为自动检查点 + “进度已保存”确认：点击确定回到当前剧情，退出箭头和剧情回顾入口不丢失；直接退出仍返回主菜单。
- 退出箭头 RectTransform 对齐 Inspector：右下锚点/轴心、坐标 0、尺寸 67.701×69.2；终局隐藏退出入口，只保留本局复盘与战绩报告。
- 军议邀约改为三项选择下方的 750×80、65 号按钮；战绩报告趋势区显示四项资源进度条及当前数值，史实简注字号与倾向说明统一并居中。
- `VerifyPhase4.ps1` 再次全部通过；因当前没有 Unity Game 视图句柄，仍待用户在编辑器中做最终视觉确认。

## 2026-09-10 · 第四轮截图反馈代码收口

- 首页开始游戏改为较浅棕色；保存确认弹窗的确定按钮改为标题正下方的单列布局。
- 结局节点恢复右下角退出入口，退出弹窗按状态显示“再玩一局”或“保存进度”；复盘/战绩报告打开时临时让出退出入口，关闭后恢复。
- 剧情回顾关闭后重新判断当前决策点，军议邀约对五条路线统一恢复；按钮缩短为 500×80 并强制以 38 号深色文字置顶显示。
- 回顾卡片从已有对白背景图集合中轮换素材；战绩报告结局摘要取消可能产生省略号的评价拼接，资源条改为带可见左标签和右侧百分比的独立行。
- `VerifyPhase4.ps1` 已通过：定向 UI 编译、36 条章节检查、战绩分析、存档/首页/设置/运行日志/报告契约全部通过；Unity Game 视图仍需用户最终确认。

## 2026-09-10 · 第五轮截图反馈代码收口

- 首页开始游戏文字改为白色。
- 军议邀约缩至 400×80，字号改为与剧情选项一致的 40 号。
- 修复结局页直接再玩一局时未恢复 DialoguePanel、Sidebar、ResourcePanel 的生命周期 bug；新局开始前统一恢复剧情输入层、资源层和点击继续状态。
- `VerifyPhase4.ps1` 已通过全部检查；仍需 Unity Game 视图复测“结局 → 再玩一局 → 五张背景介绍 → 首个角色对白”的完整路径。

## 2026-09-10 · 第六轮截图反馈代码收口

- 定位并修复 `ButtonSpriteSwap` 在 `Start`/鼠标移出时把首页“开始游戏”文字重置为黑色的问题；`HomeHubUI` 现在同步覆盖其普通与悬停颜色，并对所有 TMP 子节点强制白字。
- 军议邀约曾尝试以 `.525` 锚点兜底，但最新截图证明运行时未完成布局时仍会偏离；现改回 `.44` 作为布局前的合理兜底，并在 `LateUpdate` 持续按 `OptionButton2/3` 实测间距重定位。
- 发现首页黑字还会被场景序列化值覆盖；已同步把 `StartButton` 的 TMP 字色和 `ButtonSpriteSwap.normalColor` 写成白色，并在 `HomeHubUI.LateUpdate` 中做运行时兜底。
- 由于附件仍显示黑字，进一步把 `StartButtonLabelColorFix` 挂到场景按钮本身，独立覆盖旧状态脚本，避免 `HomeHubUI` 初始化顺序影响结果；军议邀约改为运行时读取 `OptionButton2/3` 的实际中心距定位，不再依赖固定锚点猜测。
- 2026-09-11：按最新截图反馈将军议邀约收窄为 300×80，并在显示时从 `OptionButton1` 读取字号、字重、字距和行距；静态合同检查重新通过。当前无 Unity Game 视图可用，仍待实机截图确认视觉结果。
- 2026-09-11（本轮复核）：截图仍显示旧 `.40` 锚点和黑字；确认 Unity 启动实际卡在 Sentinel LDK 授权窗口，`Assembly-CSharp.dll` 未刷新。将邀约布局前的静态兜底改回 `.44`，并在 `LateUpdate` 持续重试实测对齐；白字守护组件提升为 `DefaultExecutionOrder(10000)`，静态检查再次全部通过。
- `VerifyPhase4.ps1` 需在本轮代码变更后重跑；当前仍处于 `awaiting_visual_verification`，待 Unity Game 视图确认两张最新截图与完整路线。
- 复核发现原 Unity 实例的 `Assembly-CSharp.dll` 仍是旧时间戳，解释了截图持续显示旧黑字/旧间距；已关闭无界面旧实例并移走确认过的零字节锁文件，但批处理 Unity 在启动/许可阶段没有产生日志，未能取得新的运行截图。当前不把静态检查冒充实机验收。
- 首页合同检查现额外读取场景 YAML，直接断言 `StartButton` 的 TMP 字色与 `ButtonSpriteSwap.normalColor` 均为白色。
- 解除上一轮阻塞后的新尝试：为避免旧程序集/旧状态组件在场景重新载入时再次写黑字，已将 `StartButton` 专属 `ButtonSpriteSwap` 序列化为禁用；原生 Button 的点击与颜色过渡仍保留，QA 新增禁用断言。

## 2026-09-11 · 第七轮回放路径修复

- 终局“再玩一局”改走 `FinalUIManager.StartReplay()`，直接开始新局，不再重新叠加首页身份说明弹层。
- 重开时显式恢复 `DialoguePanel`、`Sidebar`、`ResourcePanel`、对白点击层和背景介绍点击层，重新绑定运行时监听并清除 EventSystem 选中状态；首个角色对白将继续显示可点击推进层。
- `FinalUIManager.OnDialogueNodeShown` 将点击层置于对白图形之后，避免回放时首个对白节点被不可交互图形遮住。
- `VerifyPhase4.ps1` 再次通过全部定向检查；Unity Game 视图仍待可操作窗口或用户截图做最终视觉/路线验收。

## 2026-09-11 · 当前轮截图复核

- 最新附件仍对应旧运行时：开始游戏黑字、军议邀约保留旧间距；源码已分别由 StartButton 白字守护和按选项实测间距逻辑覆盖。
- 使用 `pwsh` 重跑 `VerifyPhase4.ps1`，定向编译、36 条章节检查、存档/首页/设置/运行日志/战绩报告契约全部通过；当前阶段仍为 4B 体验整合收口，实机 Game 视图待 Unity 刷新程序集后确认。
- 为避免旧程序集或场景状态再次启用 `ButtonSpriteSwap`，已从 `StartButton` 场景对象移除该遗留组件，保留原生 `Button`、序列化白色 TMP 和专属白字守护；QA 合同同步允许“组件不存在”这一更强约束。
- Unity 编辑器已重新载入场景并实际运行首页，开始游戏文字确认显示为白色；运行到下一步后编辑器窗口被系统最小化，军议邀约决策节点暂未取得新的实机截图，因此不提前标记视觉验收完成。
- 随后完成一条最新实机路径复核：首页白字、五张背景介绍、首个角色对白、退出与剧情回顾入口均正常；首个决策点实测军议邀约文字可见、按钮收窄且与第三选项保持一致间距。七条结局线的完整回放仍需继续抽样。
- Unity 运行程序集已刷新至 2026-09-11 17:52；最新 Game 视图实测与源码一致，前述两项截图反馈不再是待验证状态。阶段 4B 仍保留七条结局线的完整回放抽样作为最终收口检查。

## 2026-09-11 · 4C 本机参谋建议闭环第一增量

- 按用户最新要求移除存档确认提示末尾的句号，文案改为“已保存到自动检查点，点击确定继续游戏”。
- 新增 `InviteSessionState` 运行时状态机：`AwaitingGuest`、`GuestSubmitted`、`Accepted`、`Declined`、`Closed`；邀请码由当前幕次、节点、路线和资源快照确定性生成，并绑定本局/节点状态。
- “军议邀约”快照新增“本机应邀加入”入口。参谋可输入接力码、昵称，选择当前剧情选项并提交一句理由；错误码、空字段和重复提交均不会触碰主线。
- 主将审核面板支持采纳、拒绝和撤销。采纳/拒绝只生成运行时“共谋回声”，实际剧情仍必须由主将点击原选项推进，不重复资源结算。
- `RunHistoryData`/`RunHistoryTracker` 新增 advisor echo 记录，战绩报告新增“共谋回声”卡片；新局、读档、返回主菜单和重置会清理邀约状态。
- `VerifyPhase4.ps1` 新增邀约闭环契约检查，定向编译、36 条章节检查、存档/首页/设置/运行日志/报告和邀约契约全部通过。下一停点为 Unity Game 视图烟测及五锚点/终局回归。

## 2026-09-11 · 4C 邀约文案与回声排版微调

- 按最新截图删除邀请卡接力码后的“（仅当前运行实例，离线）”，并移除应邀页标题中的“· 本机离线接力”、说明段和底部网络提示；保留必要的接力码、昵称、选项和理由输入。
- 战绩报告“共谋回声”正文改为居中、34 号，与“史官简注/史实结局”正文保持同一字号标尺。
- `VerifyPhase4.ps1` 再次通过定向编译、状态生命周期、36 条章节、存档、首页设置、运行日志、报告和邀约契约检查。

## 2026-09-11 · 4C 第二次邀约输入修复

- 修复应邀面板第二次打开后昵称/理由输入框无法获得焦点的问题：输入框补齐 `targetGraphic`、保持可交互，并在每次打开时清理 EventSystem 旧选中对象、重新激活输入状态。
- 理由改为可选；现在只填写昵称并选择建议即可提交，校验提示改为“请填写昵称并选择建议”，末尾不带句号。
- 增加空理由状态生命周期检查，并确认旧强制理由提示和已删除的离线说明不会回到 UI 源码。
- 追加稳态处理：应邀面板现在先激活、再刷新画布、最后重置 TMP 输入框；关闭面板/回顾时清理 EventSystem 选中对象，避免二次打开继承失效焦点。
- 状态机将 `null` 理由统一规范化为空字符串，和界面空白输入保持同一可选语义；QA 同时覆盖空值提交与回声生成。

## 2026-09-11 · 4C 回声与报告排版修正

- 共谋回声卡中有理由时改为从“理由：”单独起行；战绩报告曹操头像旁的结局摘要改为水平居中。
- `BattleReportContractChecks.ps1` 新增两项排版契约；`VerifyPhase4.ps1` 全部通过。
- 结局摘要同时显式设置垂直居中，避免不同结局文本长度造成头像旁上下偏移。
- 阶段推进审计：4C 计划仍为 `awaiting_visual_verification`，Unity 邀约闭环烟测与五锚点/终局回归尚未完成，因此暂不切换到路线图阶段 05。
- 状态机回归扩展至 1001/2001/3001/4001/5001 五个真实决策锚点，覆盖 3/2/3/2/3 选项形状、空理由提交、采纳与重置；全套 QA 仍通过。
- Unity Game 视图已恢复并显示最新战绩报告：曹操头像旁的“撤退失序，官渡防线崩溃，曹军被迫退回许都。”已实际呈居中；邀约回声换行仍以源码/契约验证，待进入含回声的局内路径复核。

## 2026-09-11 · 4C Unity 邀约闭环烟测

- Play Mode 实测首个决策锚点：打开“军议邀约”→“本机应邀加入”，昵称输入框可输入“测试参谋”；选择建议后不填写理由也能提交。
- “主将审核”正确显示“理由：未填写”，点击“采纳建议”后记录共谋回声；关闭审核弹层返回原决策界面，选项、“军议邀约”和“剧情回顾”入口均仍可用。
- 本次烟测覆盖阶段四计划中的邀约闭环项；五个锚点与七条结局的完整视觉回放仍未完成，路线图阶段 05 继续保持“待开始”。
- Unity Play Mode 追加一条真实终局路径回归（首个决策选择“伴装败退，设下埋伏”并完成诱敌滑杆）；战绩报告实机显示曹操头像旁完整摘要“撤退失序，官渡防线崩溃，曹军被迫退回许都。”，文本未被省略且水平居中，资源趋势右侧百分比同步可见。该证据覆盖单条终局路径，不能替代五锚点与七结局完整回放。
- 2026-09-12：在同一 Unity Play Mode 会话继续完成 2001 第二项“粮草乃根本，不如暂回许都，再图后举”路线，经过 2003x 终局叙事页进入终局菜单；打开战绩报告实机确认头像旁显示完整“撤退失序，官渡防线崩溃，曹军被迫退回许都。”，无省略号且在卡片内居中，兵力/粮草/计策等趋势条右侧显示 35%、35%、50% 等当前百分比。该路径新增第二锚点分支证据，但仍不能替代五锚点与七结局完整回放。
- 2026-09-12：从该终局的退出入口打开“本局已结束，要再玩一局吗？”并点击“再玩一局”，重新经过背景介绍页后首个角色对白和地图资源界面均可继续点击；未复现此前重开后首个对白节点卡死的问题，终局退出入口在结局菜单仍保留。

## 2026-09-13 · 趋势可视化详情回归

- 根据最新 Game 视图截图确认趋势卡标题、图例和终点读数可见，但折线/网格区域为空白；新增 FR-087，要求卡片内有可见预览并提供更大详情弹层。
- 新增 `ResourceTrendPlotUI`，在布局完成后的 `LateUpdate` 里用原生 UI Image 绘制网格、坐标轴、多资源折线和节点，避免自定义 `MaskableGraphic` 受 ScrollRect 布局时序影响。
- 趋势卡扩高到 500 UI 单位，加入“查看详情”；详情弹层展示同一份多点时间线、图例、终点读数和坐标说明，关闭只隐藏弹层，不改报告滚动位置。
- `VerifyPhase4.ps1` 首次因定向编译清单漏列新渲染器而失败，补列后再次通过定向编译、36 条章节检查、分析器、状态、存档、首页、运行历史、战报和邀约契约；Unity Game 视图仍待复核。

## 2026-09-13 · 趋势图信息表达完善

- 根据用户提供的完整趋势图改造要求，将资源图拆为 `ResourceTrendViewMode.Compact` 与 `Detail`，共享 `BuildTrendTimeline`，分别处理信息密度和布局边距。
- Detail 增加 0/25/50/75/100 纵轴刻度、真实快照标签横轴、风险语义提示、不同 marker 和风险虚线；Compact 显示起点/终点及最多 3 个中间标签，正文保留终点相对起点的动态差值。
- 图例改为与折线 marker 对应的符号，标题启用自适应字号；趋势绘制根据 `RectTransform.rect` 计算边距，未修改 Canvas Scaler、资源计算、剧情逻辑或存档结构。
- `VerifyPhase4.ps1` 再次通过：定向编译、36 条章节、分析器、状态、存档、首页、运行历史、战报和邀约检查全部通过；1920×1080/1600×900 的实际 Game 视图仍需手动确认。

## 2026-09-13 · 趋势图第二轮视觉精修

- 新增 FR-088，基于用户第二轮反馈优化趋势图信息层级与卡片空间，不改变真实 `resourceTimeline` 或资源计算。
- 普通报告卡左侧预留位收窄至 `.16`，正文起点统一为 `.18`；Compact 改为标题、终点摘要、单行图例/弱化风险提示、折线图顺序，绘图区扩展到 `.18–.975`。
- Detail 图表扩展到 `.19–.78`，风险说明只保留一处，底部仅保留 17 号弱化数据来源；终点摘要统一为两行两列资源读数。
- X 轴中间节点按决策关键词生成短事件名，未知长标题回退“决策N”且不产生省略号；风险线保持真实位置，使用虚线与空心方块 marker。`pwsh Documentation/QA/VerifyPhase4.ps1` 与 `git diff --check` 通过；Game 视图仍待 Unity 重新导入后确认。

## 2026-09-13 · 战绩报告打开回归修复

- Unity Editor.log 定位到确定异常：`BattleReportUI.CreateLegend()` 对尚未完成 TMP 材质初始化的图例文本设置 `outlineWidth`，触发 `TextMeshProUGUI.SetOutlineThickness()` 的 `NullReferenceException`，使 `Open()` 在 `overlay.SetActive(true)` 之前中断。
- 移除报告图例与 `ResourceTrendPlotUI` 趋势节点 marker 的运行时描边赋值；保留颜色、形状、粗体 marker 区分，不改变趋势数据和布局。
- `pwsh Documentation/QA/VerifyPhase4.ps1` 通过全部定向编译、36 条章节、分析器、状态、存档、首页、运行历史、战报和邀约检查；本机当前无可操作 Unity Game 窗口，需编辑器重新导入后做最终点击验收。

## 2026-09-13 · 趋势图第三轮视觉尺度收口

- 按用户第三轮反馈，把 Compact/Detail 的图例和终点值合并为四项资源摘要：颜色/形状 marker、当前百分比和相对起点变化量同处一项；删除 Compact 独立图例、终点摘要标题、风险说明，以及 Detail 独立终点状态和来源说明。
- Compact 现为标题 → 四项摘要 → 大面积折线预览 →“查看详情”；Detail 现为标题 → 四项摘要 → 单次风险语义 → 大面积多节点折线。坐标轴保留真实节点短标签，风险线仍按真实坐标绘制为虚线并使用空心方块 marker。
- 放大趋势文字：Compact 标题 40、摘要 27、Y/X 轴 22/20；Detail 标题 46、摘要 29、Y/X 轴 24/22；详情按钮 26、关闭按钮 32。趋势轴标签关闭 TMP 自动缩小，项目字体仍为 `SC-Regular SDF`。
- 运行 `pwsh Documentation/QA/VerifyPhase4.ps1` 通过定向编译、36 条章节、分析器、状态、存档、首页、运行历史、战报和邀约检查；`git diff --check` 无空白错误。场景 Canvas Scaler 原配置为 1920×1080 / Match 0.5，趋势代码没有父级缩放压缩；Unity Game View 当前不可操作，最终实机可读性仍需编辑器或用户截图确认。

## 2026-09-13 · 趋势图最后一轮可读性精修

- 按用户确认的收口方案删除 Detail 的“风险数值越高，局势越危险”，不再增加解释、tooltip、动画或其他功能。
- 横轴字号调整为 Detail 26 / Compact 24，纵轴调整为 Detail 27 / Compact 25；坐标标签改用更亮但非纯白的灰白色，并使用 TMP Medium 字重，标签位置下移以增加与轴线的间距。
- Detail 删除风险提示后的绘图区上边界调整到 `.72`；Compact 卡片从 520 增高到 560，绘图区改为 `.12–.70`，详情按钮上移到 `.045–.115` 并调整为 28 号。按实际 UI 高度估算 Compact 折线绘图区比上一版增加约 18%。
- `pwsh Documentation/QA/VerifyPhase4.ps1` 全部通过，`git diff --check` 通过；数据逻辑、线条配色、marker、资源摘要结构和字体资产均未更换。Unity Game View 仍不可操作，最终录屏可读性保留为待人工确认。

## 2026-09-13 · 坐标文字最终放大

- 根据用户对比截图反馈，停止调整图表布局，只放大坐标文字：Detail X/Y 为 32/31，Compact X/Y 为 30/29；坐标字重从 Medium 提升为 SemiBold，颜色调整为更接近资源摘要的高亮灰白但不使用纯白。
- 没有修改图例/摘要结构、图表高度、折线、marker、左侧卡片宽度、风险提示或资源数据逻辑。
- `pwsh Documentation/QA/VerifyPhase4.ps1` 通过全部定向编译与契约检查；Unity Game View 仍不可操作，16:9 / 1.5x 录屏可读性需要人工最终确认。
