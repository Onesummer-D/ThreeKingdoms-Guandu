# 4D 文化深化与全流程回归

Status: awaiting_unity_regression
Active Subtask: unity-regression

## 目标

完成阶段四最后一块未收口的内容：把五个决策锚点、七个结局、500215 彩蛋、史官注和报告文化说明汇总为一套可执行的回归证据，并修正会把架空分支说成史实结局的文案。当前版本只有本机离线邀约，没有远端服务；在线联机不作为已完成能力宣称。

## 范围与约束

- 覆盖五锚点 `1001/2001/3001/4001/5001`、七个终止结局 `200314/300416/500217/500313/500320/500411/500418`、对应解锁节点和 500215 彩蛋。
- 不修改对白节点、资源结算、结局判定或 ScriptableObject 结构；文化说明和 QA 文档/脚本可以更新。
- 史官注的四层结构必须保持为出处、史书记载/通行译述、今天如何理解、游戏改编说明；改编内容不得冒充原文。
- 代码检查不能代替 Unity Game 视图；不能把“远端联机”写成已验收。

## 实施步骤

1. [x] 盘点五锚点、七结局、解锁节点和彩蛋触发链，确认当前映射与缺口。验证：源码、对白资产和现有 QA 映射交叉核对。
2. [x] 修正七结局文化说明的史实/架空边界，并补充 4D 文化核对表。验证：史实胜利单独标为史实结局，其余六条明确写出史实背景与本局架空分支。
3. [x] 新增数据驱动 `Stage4DCoverageChecks.ps1`，检查五锚点四字段、七结局全链路映射、七条评价标签唯一性、彩蛋触发/资源/回顾勋章链和长文案无省略号。验证：脚本通过；条件/小游戏分流保留给 Unity Play Mode。
4. [x] 把 4D 检查接入 `VerifyPhase4.ps1`，并新增人工回归清单，区分已执行、待 Unity 实机和未部署在线能力。验证：总检查通过。
5. [x] 运行完整阶段四检查、静态差异检查和 4D 覆盖检查；记录证据与剩余实机门槛。验证：`VerifyPhase4.ps1` 退出码 0，`git diff --check` 无错误。
6. [x] 完成计划合规表；五锚点/七结局静态覆盖和可执行清单已齐全，Unity 全路线仍按证据边界保留为待实机，不宣称已完成。

## 验收映射

| Req ID | Requirement Summary | Status | Verification |
|---|---|---|---|
| FR-036 | 七条结局分别有评价并仅在对应结局显示 | ✓ met | `Stage4DCoverageChecks.ps1` 检查七个终止 ID、独立评价和唯一标题；终局显示门槛由既有契约覆盖 |
| FR-037 | 五锚点史官注四字段 | ✓ met | `Stage4DCoverageChecks.ps1` 检查五个可选节点和每个 `HistorianNote` 四行字段；文化核对表记录来源边界 |
| FR-039 | 普通回顾、终局复盘、关闭、新局不泄漏 | ~ partial | 现有 3C/4C 契约；待 4D 清单汇总 |
| FR-046 | 邀约与终局复盘的阶段四回归 | ~ partial | 4C 状态检查与 4D 终点/运行时分流契约通过；五锚点/七结局点击回放待执行 |
| FR-053 | 五锚点、七结局和彩蛋文化边界可核对 | ✓ met | 4D 文化核对表、七结局史实/架空文案和覆盖脚本已落盘；正式参赛素材授权仍按台账另行补证 |
| NFR-034 | 离线 Windows 可演示 | ~ partial | 定向编译和契约检查通过；Unity 全路线实机仍待确认 |
| NFR-037 | 不伪造在线服务/账号/二维码能力 | ✓ met | 4D 文档明确远端服务未部署，检查禁止在线能力宣称 |

**Compliance Status: PARTIAL**

## Errors

| Attempt | Error | Resolution |
|---|---|---|
| 1 | 计划恢复时误从仓库根目录读取 `.planning`，实际计划根在 Unity 子项目 | 固定使用 `Unity/官渡之战` 作为本计划根，未产生生产改动 |
| 2 | 首次执行 4D PowerShell 覆盖脚本时，双引号字符串中的 `$ending:` 被 PowerShell 误解析为非法变量引用 | 将相关插值改为 `${ending}`/`${unlock}` 形式后重新执行；未产生生产代码回退 |
| 3 | 节点块解析用 `nodeId: 1001` 前缀匹配到了 `100101`，误报锚点无选项 | 将节点标记改为带换行边界的精确匹配，再重跑覆盖脚本 |
| 4 | 史官注第四字段是构造函数的位置参数，正文不一定包含“改编”字样，脚本因此误报字段缺失 | 改为检查构造块中四行字符串参数，并保留来源/游戏解释关键词检查 |
| 5 | 最后一个史官注字典项以 `}` 结束而非 `},`，覆盖脚本的块结束标记过窄 | 改为按字典项通用闭合行 `}` 截取，并重新执行 |
| 6 | 结局解锁节点会先进入多段终局叙事，再到终止节点，脚本误要求解锁节点一跳直达终点 | 改为沿无选项 `nextNodeId` 链最多追踪 32 步，验证最终抵达对应终止节点 |
| 7 | 尝试从 100001 用静态 YAML `nextNodeId` 图证明七个终点全部可达时，500217 被运行时条件/小游戏分流挡住，静态图不能还原完整路线 | 移除会误报的静态 BFS；保留终点节点、解锁叙事链和代码映射检查，完整带条件路线列为 Unity Play Mode 证据 |
| 8 | 增加结局标题唯一性检查时误用 C# 风格 `\"` 作为 PowerShell 双引号转义，脚本解析失败 | 改用单引号正则模板拼接节点 ID，再重跑脚本 |
| 9 | 只读检查未发现当前运行中的 Unity/UnityHub 进程，因此无法在本轮直接执行 Unity Game 视图五锚点/七结局点击回放 | 保留人工回归清单和静态证据边界，等待 Unity 编辑器重新打开后继续执行，不将静态通过标为实机通过 |
| 10 | 新增运行时分流契约时，Windows PowerShell 5 对包含方法标记/比较符号的检查字符串解析失败，随后又暴露 UTF-8 无 BOM 脚本中的中文字符串匹配问题 | 改为稳定片段检查、用字符码构造文化标签，并改用 Windows PowerShell 5 重跑；覆盖脚本现已通过 |
| 11 | 用 Windows PowerShell 5 跑完整 `VerifyPhase4.ps1` 时，原有存档/首页/运行日志/战报/邀约契约脚本中的 UTF-8 中文字面量也会触发解析或匹配失败 | 将这些 QA 脚本的中文检查值改为字符码构造，保持检查语义不变；完整阶段四总验收随后通过 |
| 12 | 本轮再次通过 Windows 应用状态与本机常见 Unity 安装路径检查，未发现可操作 Unity/UnityHub 窗口或可直接启动的 Unity 编辑器目标 | 不修改 4D 证据边界；继续保持 `awaiting_unity_regression`，下次编辑器可用时直接执行既有五锚点/七结局清单 |
| 13 | 初版运行时分流检查只验证全文件中存在阈值变量和目标节点，不能证明 500308/500406 的完整条件表达式对应正确分支 | 改为按两个方法边界检查完整阈值、fallback 和四个目标节点；4D 覆盖与阶段四总验收再次通过 |
